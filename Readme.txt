Run the program with the following:
open a terminal: [in Linux/OSX]
#install haskell

sudo apt install haskell-platform
chmod +x run.sh
./run.sh


_______________________________________

FileReader.hs - implements all the functions regarding to parsing string text and pattern matching to find substrings in newline delimited sentences.
Main.hs - is entry point of the program.
